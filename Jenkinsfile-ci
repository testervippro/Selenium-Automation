pipeline {
    agent any

    environment {
        mvnHome = ''
    }

    stages {
        stage('Preparation') {
            steps {
                script {
                    // Set mvnHome in the Preparation stage
                    mvnHome = tool 'maven'  // 'maven' is the name you assigned to your Maven installation in Jenkins' Global Tool Configuration
                    echo "Maven Home: ${mvnHome}"
                }
            }
        }

        stage('Build') {
            steps {
                script {
                    // Use the mvnHome variable to run Maven build
                    sh "'${mvnHome}/bin/mvn' clean package -DskipTests=true"
                }
            }
        }

        stage('Test Execution') {
            steps {
                script {
                    // Use the mvnHome variable to run Maven test
                    sh "'${mvnHome}/bin/mvn' test -Pweb-execution -Dsuite=local-suite -Dtarget=local-suite -Dheadless=true -Dbrowser=chrome"
                }
            }
        }
    }

    post {
        always {
            // Generate Allure report
            allure([
                results: [[path: 'target/allure-results']],
                reportBuildPolicy: 'ALWAYS',
                includeProperties: false
            ])
            
            // Send an email notification with Allure report link
            mail(
                to: 'cuxuanthoai@gmail.com',
                subject: "Jenkins Build - ${currentBuild.currentResult}",
                body: """
                    The Jenkins build ${env.JOB_NAME} (#${env.BUILD_NUMBER}) has completed.
                    Status: ${currentBuild.currentResult}
                    Jekins Server :https://ed1f-116-96-46-98.ngrok-free.app
                    Allure Report: https://ed1f-116-96-46-98.ngrok-free.app/job/${env.JOB_NAME}/${env.BUILD_NUMBER}/allure
                    Build URL: ${env.BUILD_URL}
                """
            )
        }

        success {
            echo 'Tests completed successfully!'
        }

        failure {
            echo 'Tests failed!'
        }
    }
}
